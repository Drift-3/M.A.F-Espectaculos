const HeroSection = () => (
  <section className="flex items-center justify-center h-screen bg-gray-900 text-white">
    <h1 className="text-4xl font-bold">¡Bienvenido a tu sitio web!</h1>
  </section>
);

export default HeroSection;
